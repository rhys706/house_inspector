name: house_inspector
description: A house inspection report app.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+5
  speech_to_text: 7.3.0 # Alternative stable version
  permission_handler: ^11.0.1
  path_provider: ^2.1.1
  pdf: ^3.10.4
  printing: ^5.11.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true